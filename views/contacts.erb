<body>
	<h1>All Contacts</h1>
	<hr class="style-eight"></hr>
	<ul>
		<% @@rolodex.contacts.each do |contact| %>
		<li><%= contact.first_name %> <%= contact.last_name %> 
			<%= contact.email %> <%= contact.note %></li>
		<% end %>
	</ul>
</body>